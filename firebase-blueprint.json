{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Represents a user account and their role in the system.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["customer", "admin", "rider"] },
        "created_at": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "MenuItem": {
      "title": "Bakery Item",
      "description": "An item on the bakery menu.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "price": { "type": "number" },
        "category": { "type": "string" },
        "image": { "type": "string" },
        "available": { "type": "boolean" },
        "description": { "type": "string" },
        "stock_quantity": { "type": "number" }
      },
      "required": ["name", "price", "category", "available", "stock_quantity"]
    },
    "Order": {
      "title": "Customer Order",
      "description": "Details of a customer purchase.",
      "type": "object",
      "properties": {
        "user_id": { "type": "string" },
        "customer_name": { "type": "string" },
        "phone": { "type": "string" },
        "items": { "type": "array" },
        "total": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "confirmed", "assigned", "preparing", "out_for_delivery", "delivered", "cancelled"] },
        "address": { "type": "string" },
        "payment_method": { "type": "string" },
        "utr": { "type": "string" },
        "payment_status": { "type": "string" },
        "order_id": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" },
        "updated_at": { "type": "string", "format": "date-time" }
      },
      "required": ["user_id", "customer_name", "phone", "items", "total", "status", "address"]
    },
    "Rider": {
      "title": "Delivery Rider",
      "description": "A delivery partner.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "status": { "type": "string", "enum": ["online", "busy", "offline"] },
        "current_order_id": { "type": "string" }
      },
      "required": ["uid", "name", "status"]
    },
    "AppConfig": {
      "title": "App Configuration",
      "description": "Global settings for the application.",
      "type": "object",
      "properties": {
        "isOrderingOpen": { "type": "boolean" },
        "updated_at": { "type": "string", "format": "date-time" }
      },
      "required": ["isOrderingOpen"]
    },
    "ThemeSettings": {
      "title": "Theme Settings",
      "description": "Visual customization and global branding for the app.",
      "type": "object",
      "properties": {
        "primaryColor": { "type": "string" },
        "secondaryColor": { "type": "string" },
        "accentColor": { "type": "string" },
        "backgroundColor": { "type": "string" },
        "surfaceColor": { "type": "string" },
        "fontFamily": { "type": "string" },
        "borderRadius": { "type": "string" },
        "darkMode": { "type": "boolean" },
        "offerText": { "type": "string" },
        "showOfferBanner": { "type": "boolean" },
        "offerLink": { "type": "string" },
        "offerColor": { "type": "string" }
      }
    },
    "Coupon": {
      "title": "Discount Coupon",
      "description": "Promotional codes for discounts.",
      "type": "object",
      "properties": {
        "code": { "type": "string" },
        "type": { "type": "string", "enum": ["percentage", "fixed"] },
        "value": { "type": "number" },
        "min_order": { "type": "number" },
        "expiry_date": { "type": "string", "format": "date" },
        "usage_limit": { "type": "number" },
        "usage_count": { "type": "number" },
        "status": { "type": "string", "enum": ["active", "expired", "disabled"] },
        "is_first_order_only": { "type": "boolean" },
        "created_at": { "type": "string", "format": "date-time" }
      },
      "required": ["code", "type", "value", "min_order", "expiry_date", "usage_limit", "usage_count", "status"]
    },
    "Review": {
      "title": "Order Review",
      "description": "Feedback from a customer about their order.",
      "type": "object",
      "properties": {
        "order_id": { "type": "string" },
        "user_id": { "type": "string" },
        "customer_name": { "type": "string" },
        "rating": { "type": "number" },
        "comment": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" }
      },
      "required": ["order_id", "user_id", "customer_name", "rating", "comment", "created_at"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "User",
      "description": "Storage for user profile data."
    },
    "menu/{itemId}": {
      "schema": "MenuItem",
      "description": "Bakery menu items."
    },
    "orders/{orderId}": {
      "schema": "Order",
      "description": "Customer order records."
    },
    "riders/{riderId}": {
      "schema": "Rider",
      "description": "Rider tracking and availability."
    },
    "settings/appConfig": {
      "schema": "AppConfig",
      "description": "Global application state."
    },
    "settings/theme": {
      "schema": "ThemeSettings",
      "description": "Global theme configuration."
    },
    "coupons/{couponId}": {
      "schema": "Coupon",
      "description": "Promotional discount codes."
    },
    "reviews/{reviewId}": {
      "schema": "Review",
      "description": "Customer feedback and ratings."
    }
  }
}
